services:
  bvl-sales:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      DATA_DIR: /app/data
    volumes:
      - sales-data:/app/data
    restart: unless-stopped

volumes:
  sales-data:
